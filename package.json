{
  "private": true,
  "scripts": {
    "lint": "run-p lint:commits format",
    "lint:commits": "commitlint --from --",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@commitlint/config-conventional": "19.6.0",
    "commitlint": "19.5.0",
    "npm-run-all2": "^6.1.2",
    "prettier": "3.1.0"
  }
}
